// Functions that renders presentable text on the console

function searchPresentation(target, array, output) {
    console.log(`~Target: ${target}\n~Array: [${array}]\n~Found at: ${output}`)
}

module.exports = searchPresentation